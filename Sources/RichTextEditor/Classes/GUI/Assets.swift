import Foundation
import UIKit

class Assets {

    public static var playIcon: UIImage {
//        let bundle = Bundle.rteBundle
        let playImage = UIImage(named: "play", in: .module, compatibleWith: nil)!
        return playImage
    }

    public static var imageIcon: UIImage {
//        let bundle = Bundle.rteBundle
        let playImage = UIImage(named: "image", in: .module, compatibleWith: nil)!
        return playImage
    }
}
